#pragma once

#include <iostream>
#include <string>
#include <algorithm>

namespace lettersConverter {
  std::string onlyFirstLetterUpperCase(std::string);
  std::string everyLetterToUpperCase(std::string);
  std::string everyLetterToLowerCase(std::string);
};
